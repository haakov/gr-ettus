# auto-generated by grc.converter

id: uhd_rfnoc_streamer_moving_avg
label: 'RFNoC: Moving Average'

parameters:
-   id: length
    label: Average length
    dtype: int
    default: '10'
-   id: block_index
    label: Block Select
    category: RFNoC Config
    dtype: int
    default: '-1'
    hide: ${ ('part' if int(block_index) < 0 else 'none') }
-   id: device_index
    label: Device Select
    category: RFNoC Config
    dtype: int
    default: '-1'
    hide: ${ ('part' if int(device_index) < 0 else 'none') }
-   id: fpga_module_name
    label: FPGA Module Name
    category: RFNoC Config
    dtype: string
    default: noc_block_moving_avg
    hide: all

inputs:
-   domain: stream
    dtype: complex
-   domain: message
    id: rfnoc
    optional: true

outputs:
-   domain: stream
    dtype: complex
asserts:
- ${ length in range(1, 256) }

templates:
    imports: import ettus
    make: "ettus.rfnoc_generic(\n    self.device3,\n    uhd.stream_args( \\# TX Stream\
        \ Args\n        cpu_format=\"fc32\", \\# TODO: This must be made an option\n\
        \        otw_format=\"sc16\",\n        args=\"\",\n    ),\n    uhd.stream_args(\
        \ \\# RX Stream Args\n        cpu_format=\"fc32\", \\# TODO: This must be\
        \ made an option\n        otw_format=\"sc16\",\n        args=\"\",\n    ),\n\
        \    \"MovingAverage\", ${block_index}, ${device_index},\n)\nself.${id}.set_arg(\"\
        length\", ${length})\n  "
    callbacks:
    - set_arg("length", ${length})

file_format: 1
